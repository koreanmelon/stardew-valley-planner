class Square {

    /**
     * Creates a Square object.
     * @param {Number} x
     * @param {Number} y
     */
    constructor(x, y) {
        this.x = x;
        this.y = y;
        this.hasObject = false;
    }

}

module.export = Square;
