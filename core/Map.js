const maps = require("./MAP_TYPES.js");

class Map {
    /**
     * Initializes a map based on the farmtype specified.
     * @param {String} farmType 
     */
    constructor(farmType) {
        if (farmType.toLowerCase() == "standard") {
            

 else {}
            throw new Error("invalid farm type");
        }

m.export = Map;
